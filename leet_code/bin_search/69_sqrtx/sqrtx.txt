另外,这道题还有一种更快的算法——牛顿迭代法,其公式为 x(n+1) = x(n) − f (x(n))/ f′(x(n))。给
定 f(x) = x^2 − a = 0,这里的迭代公式为 x(n+1) = (x(n) + a/x(n))/2,其代码如下。

x(n+1)
=    x - (x^2 - a) / 2*x
=   (2*x^2  - x^2 + a)/2*x
=   (X^2 + a) / 2*x
=   x/2 + a/2*x